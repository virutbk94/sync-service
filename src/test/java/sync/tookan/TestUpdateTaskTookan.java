package sync.tookan;

import jdk.jfr.Description;
import org.junit.Test;

public class TestUpdateTaskTookan {
    @Test
    @Description("state (CANCEL) = status tookan (9 (Cancel))")
    public void testState(){
    }

}
